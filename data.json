{
  "rhyme": "милулечка",
  "mood": "озорное весеннее",
  "palette": [
    "#4ea8de",
    "#a2d2ff",
    "#ffafcc"
  ],
  "style": "bloom",
  "art": "orbits",
  "font": "lobster",
  "anim": "drop",
  "verse": [
    "Ты хранишь секреты, Юлечка-милулечка,",
    "Словно драгоценная, хрупкая шкатулочка."
  ],
  "verse_tts": [
    "Ты хранишь секреты, +Юлечка-мил+улечка,",
    "Словно драгоценная, хрупкая шкатулочка."
  ],
  "svg": "",
  "scene": "let pts = []; function tick() { if (pts.length === 0) { for (let i = 0; i < 40; i++) { pts.push({ x: Math.random() * W(), y: Math.random() * H(), r: Math.random() * 4 + 2, dx: (Math.random() - 0.5) * 0.6, dy: (Math.random() - 0.5) * 0.6, col: PALETTE[i % PALETTE.length], a: Math.random() }); } } ctx.clearRect(0, 0, W(), H()); let cx = W()/2, cy = H()/2; ctx.strokeStyle = PALETTE[1]; ctx.lineWidth = 0.8; ctx.globalAlpha = 0.2; ctx.beginPath(); ctx.arc(cx, cy, 150, 0, Math.PI * 2); ctx.stroke(); ctx.beginPath(); ctx.arc(cx, cy, 220, 0, Math.PI * 2); ctx.stroke(); for (let i = 0; i < pts.length; i++) { let p = pts[i]; p.x += p.dx; p.y += p.dy; if (p.x < 0 || p.x > W()) p.dx *= -1; if (p.y < 0 || p.y > H()) p.dy *= -1; p.a += 0.01; ctx.fillStyle = p.col; ctx.globalAlpha = 0.25 + Math.sin(p.a) * 0.15; ctx.beginPath(); ctx.arc(p.x, p.y, p.r + Math.sin(p.a) * 1.5, 0, Math.PI * 2); ctx.fill(); for (let j = i + 1; j < pts.length; j++) { let p2 = pts[j]; let d = Math.hypot(p.x - p2.x, p.y - p2.y); if (d < 120) { ctx.strokeStyle = p.col; ctx.globalAlpha = (1 - d/120) * 0.12; ctx.lineWidth = 0.6; ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(p2.x, p2.y); ctx.stroke(); } } } requestAnimationFrame(tick); } tick();",
  "date": "2026-07-01"
}